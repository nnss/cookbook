<%= simple_form_for(@term) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :term %>
    <%= f.input :definition %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
