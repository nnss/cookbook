<!-- % unless @comment.id.nil? % -->

<h1>Comments</h1>
<% @comments.each do |co| %>
<div class="comment" id="comment-<%= co.id %>">
  <!-- %= link_to "x", recipe_comment_path(@comment), :method => :delete, :remote => true, :confirm => "Are you sure you want to remove this comment?", :disable_with => "x", :class => 'close' % -->
  <p> <%= co.body %> </p>
  <small> By <%= co.user.name %>, at <%= co.updated_at %>.</small>
  <hr /> <hr />
</div>
<% end %>
<!-- % end % -->
